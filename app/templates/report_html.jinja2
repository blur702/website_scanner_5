<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Website Checker Report: {{ scan.original_url }}</title>
    <style>
        :root {
            --color-bg: #121212;
            --color-bg-alt: #1e1e1e;
            --color-primary: #7b68ee;
            --color-text: rgba(255, 255, 255, 0.87);
            --color-text-muted: rgba(255, 255, 255, 0.6);
            --color-border: rgba(255, 255, 255, 0.12);
            --color-error: #cf6679;
            --color-warning: #ffb74d;
            --color-info: #29b6f6;
            --color-success: #66bb6a;
            --spacing-sm: 0.5rem;
            --spacing-md: 1rem;
            --spacing-lg: 1.5rem;
            --font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            --font-family-mono: 'JetBrains Mono', monospace;
        }
        body {
            font-family: var(--font-family);
            background-color: var(--color-bg);
            color: var(--color-text);
            margin: 0;
            padding: 0;
            line-height: 1.5;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: var(--spacing-md);
        }
        header {
            padding: var(--spacing-lg);
            background-color: var(--color-bg-alt);
            border-bottom: 1px solid var(--color-border);
            margin-bottom: var(--spacing-lg);
        }
        h1, h2, h3, h4 {
            color: var(--color-text);
            margin-top: var(--spacing-lg);
            margin-bottom: var(--spacing-md);
        }
        h1 {
            font-size: 2rem;
        }
        h2 {
            font-size: 1.5rem;
            border-bottom: 1px solid var(--color-border);
            padding-bottom: var(--spacing-sm);
        }
        h3 {
            font-size: 1.2rem;
        }
        .meta-info {
            background-color: var(--color-bg-alt);
            border-radius: 4px;
            padding: var(--spacing-md);
            margin-bottom: var(--spacing-lg);
        }
        .meta-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: var(--spacing-md);
        }
        .meta-item {
            margin-bottom: var(--spacing-sm);
        }
        .meta-label {
            color: var(--color-text-muted);
            font-size: 0.9rem;
            margin-bottom: 0.25rem;
        }
        .meta-value {
            font-weight: 500;
        }
        .section {
            background-color: var(--color-bg-alt);
            border-radius: 4px;
            padding: var(--spacing-md);
            margin-bottom: var(--spacing-lg);
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: var(--spacing-lg);
        }
        th, td {
            text-align: left;
            padding: var(--spacing-sm);
            border-bottom: 1px solid var(--color-border);
        }
        th {
            background-color: rgba(255, 255, 255, 0.05);
            font-weight: 500;
        }
        .tag {
            display: inline-block;
            padding: 0.2em 0.6em;
            border-radius: 4px;
            font-size: 0.8rem;
            font-weight: 500;
            margin-right: 0.5em;
        }
        .tag-error {
            background-color: var(--color-error);
            color: white;
        }
        .tag-warning {
            background-color: var(--color-warning);
            color: black;
        }
        .tag-info {
            background-color: var(--color-info);
            color: white;
        }
        .summary-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
            gap: var(--spacing-md);
            margin-bottom: var(--spacing-lg);
        }
        .summary-card {
            background-color: rgba(255, 255, 255, 0.05);
            border-radius: 4px;
            padding: var(--spacing-md);
        }
        .summary-card h3 {
            margin-top: 0;
            font-size: 1rem;
            color: var(--color-text-muted);
        }
        .summary-value {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: var(--spacing-sm);
        }
        .collapsible {
            cursor: pointer;
            padding: var(--spacing-md);
            background-color: rgba(255, 255, 255, 0.05);
            border-radius: 4px;
            margin-bottom: var(--spacing-sm);
        }
        .collapsible-content {
            padding: var(--spacing-md);
            display: none;
            background-color: var(--color-bg-alt);
            border-radius: 4px;
            margin-bottom: var(--spacing-md);
        }
        .collapsible.active + .collapsible-content {
            display: block;
        }
        footer {
            margin-top: var(--spacing-lg);
            padding: var(--spacing-lg);
            text-align: center;
            border-top: 1px solid var(--color-border);
            color: var(--color-text-muted);
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <h1>Website Checker Report</h1>
            <div class="url">{{ scan.original_url }}</div>
        </div>
    </header>
    
    <div class="container">
        <!-- Report content will be generated dynamically based on scan data -->
        
        {% include "report_sections/overview.jinja2" %}
        
        {% if data.resources is defined %}
